# Airflow DAGs

Minimal Airflow DAGs repository using DockerOperator to run containerized data pipelines.

## Structure

```
.
├── dags/              # Airflow DAG definitions
├── configs/           # Configuration files for DAGs
└── pyproject.toml     # Python project configuration
```
